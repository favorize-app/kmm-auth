<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <data>

        <import type="android.view.View" />

        <import type="tossaro.android.auth.R" />

        <variable
            name="vm"
            type="tossaro.android.auth.app.signin.SignInViewModel" />
    </data>

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:paddingStart="18dp"
        android:paddingTop="30dp"
        android:paddingEnd="18dp"
        android:paddingBottom="34dp"
        android:visibility="@{vm.signInType == `email` ? View.VISIBLE : View.GONE}">

        <TextView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:fontFamily="@font/poppins_medium"
            android:text="@string/sign_in_email"
            android:textColor="@color/bodyText2"
            android:textSize="12sp"
            app:layout_constraintTop_toTopOf="parent" />

        <EditText
            android:id="@+id/etEmail"
            style="@style/EditText"
            android:layout_width="match_parent"
            android:layout_height="40dp"
            android:layout_marginTop="4dp"
            android:hint="@string/sign_in_email_hint"
            android:importantForAutofill="no"
            android:inputType="textEmailAddress"
            android:lines="1"
            android:saveEnabled="false"
            android:text="@={vm.email}" />

        <TextView
            android:id="@+id/tvEmailError"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="4dp"
            android:fontFamily="@font/poppins_regular"
            android:text="@{vm.emailError != null ? context.getString(vm.emailError) : ``}"
            android:textColor="@color/negative"
            android:textSize="12sp"
            android:visibility="@{vm.emailError != null ? View.VISIBLE : View.GONE}"
            app:layout_constraintTop_toBottomOf="@id/etEmail" />

        <TextView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="8dp"
            android:fontFamily="@font/poppins_medium"
            android:text="@string/sign_in_password"
            android:textColor="@color/bodyText2"
            android:textSize="12sp"
            app:layout_constraintTop_toTopOf="parent" />

        <EditText
            android:id="@+id/etPassword"
            style="@style/EditText"
            android:layout_width="match_parent"
            android:layout_height="40dp"
            android:layout_marginTop="4dp"
            android:hint="@string/sign_in_password_hint"
            android:importantForAutofill="no"
            android:inputType="textPassword"
            android:lines="1"
            android:saveEnabled="false"
            android:text="@={vm.password}" />

        <TextView
            android:id="@+id/tvPasswordError"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="4dp"
            android:fontFamily="@font/poppins_regular"
            android:text="@{vm.passwordError != null ? context.getString(vm.passwordError) : ``}"
            android:textColor="@color/negative"
            android:textSize="12sp"
            android:visibility="@{vm.passwordError != null ? View.VISIBLE : View.GONE}"
            app:layout_constraintTop_toBottomOf="@id/etPassword" />

        <androidx.appcompat.widget.AppCompatTextView
            android:id="@+id/tvForgotPasswordButton"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="end"
            android:layout_marginTop="10dp"
            android:onClick="@{()->vm.goToForgotPassword()}"
            android:text="@string/sign_in_forgot_password"
            android:textColor="@color/primary"
            android:textSize="13sp" />

        <androidx.appcompat.widget.AppCompatButton
            android:id="@+id/btnSignIn"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginStart="24dp"
            android:layout_marginTop="24dp"
            android:layout_marginEnd="24dp"
            android:background="@drawable/bg_button_primary_solid_rounded"
            android:enabled="@{vm.email != null &amp;&amp; vm.phone != null &amp;&amp; vm.emailError == null &amp;&amp; vm.passwordError == null}"
            android:fontFamily="@font/poppins_semi_bold"
            android:gravity="center"
            android:onClick="@{()->vm.signInEmail()}"
            android:padding="12dp"
            android:text="@string/sign_in_with_email"
            android:textAllCaps="false"
            android:textColor="@color/white"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toBottomOf="@id/tvForgotPasswordButton" />

        <androidx.appcompat.widget.LinearLayoutCompat
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center"
            android:layout_marginTop="20dp"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toBottomOf="@+id/btnSignIn">

            <androidx.appcompat.widget.AppCompatTextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="@string/sign_in_dont_have_account"
                android:textColor="@color/bodyText"
                android:textStyle="bold" />

            <androidx.appcompat.widget.AppCompatTextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginStart="10dp"
                android:onClick="@{()->vm.goToRegister()}"
                android:text="@string/sign_in_register"
                android:textColor="@color/primary" />

        </androidx.appcompat.widget.LinearLayoutCompat>

    </LinearLayout>

</layout>